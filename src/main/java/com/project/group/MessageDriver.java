package com.project.group;

import com.project.group.entity.Message;
import com.project.group.persistence.MessageDao;

/**
 * Created by michaelklein on 11/19/15.
 */
public class MessageDriver {

    /** Main method to execute the various crud operations
     * @param args The command line arugments - not used in this case
     **/

    public static void main(String[] args) {
    /*
        MessageDao dao = new MessageDao();
        System.out.println("The messages before running: ");
        System.out.println(dao.getMyMessageList());

        // Create a new Message object
        Message userMessageInsert = new Message(0, "Test to see if this adds a message!");
        // Call the add method on the DAO
        int roleId = dao.addMessage(userMessageInsert);

        System.out.println("The messages after insert : ");
        System.out.println(dao.getMyMessageList());
        System.out.println();

        // Update the Message that we just added
        Message userMessageUpdate = new Message(roleId, "Update the message.");
       // dao.addOrUpdateUserMessage(userMessageUpdate);

        System.out.println("The messages after update : ");
        System.out.println(dao.getMyMessageList());
        System.out.println();

        // get just one Message (the one that we just updated)
        System.out.println("Getting one record");
       // System.out.println(dao.getMyMessage(roleId));
        System.out.println();

        // Delete the message that we just updated
        dao.deleteMyMessageById(roleId);

        System.out.println("The messages after delete : ");
        System.out.println(dao.getMyMessageList());
        System.out.println();
    */
    }
}
